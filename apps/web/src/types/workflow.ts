export type WorkflowTemplate = {
  id: string
  name: string
  slug: string
  version: string
  template_path: string
  manifest_path: string
  is_enabled: boolean
  created_at: string
  updated_at: string
}
